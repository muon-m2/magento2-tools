<?xml version="1.0"?>
<!--
    ACL resource tree.
    ::main  — used as ADMIN_RESOURCE on all admin controllers.
    ::config — used as <resource> in system.xml sections.
    Add further child resources for fine-grained role control.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:Acl/etc/acl.xsd">
    <acl>
        <resources>
            <resource id="Magento_Backend::admin">
                <resource id="{Vendor}_{ModuleName}::main"
                          title="{ModuleName}"
                          sortOrder="100">
                    <resource id="{Vendor}_{ModuleName}::config"
                              title="{ModuleName} Configuration"
                              sortOrder="10"/>
                </resource>
            </resource>
        </resources>
    </acl>
</config>
