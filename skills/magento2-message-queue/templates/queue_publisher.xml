<?xml version="1.0"?>
<!--
    Message-queue publisher binding for {Vendor}_{Module}.
    Target: {Vendor}/{Module}/etc/queue_publisher.xml  (merge into existing file)

    Tokens: {TopicName}, {ConnectionName}, {ExchangeName}
    Maps the topic to the connection + exchange the publisher writes to. {TopicName} must
    match communication.xml + queue_topology.xml + the publisher's TOPIC constant;
    {ExchangeName} must match queue_topology.xml; {ConnectionName} ("db" by default) must
    match queue_topology.xml + queue_consumer.xml.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework-message-queue:etc/publisher.xsd">
    <publisher topic="{TopicName}">
        <connection name="{ConnectionName}" exchange="{ExchangeName}"/>
    </publisher>
</config>
