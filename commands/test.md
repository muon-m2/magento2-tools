---
description: Generate unit, integration, API, JS, or MFTF tests for a Magento 2 module (magento2-test-generate). Runs against missing tests only when --missing-only is set.
argument-hint: "[--types=unit,integration,api,js,mftf] [--missing-only] <Vendor>_<Module>"
---
Use the `magento2-tools:magento2-test-generate` skill, forwarding these arguments verbatim: $ARGUMENTS
