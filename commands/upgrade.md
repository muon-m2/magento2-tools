---
description: Detect BC breaks, deprecations, and required changes when upgrading a Magento 2 module to a new Magento/PHP version (magento2-module-upgrade).
argument-hint: "--to-magento=X.Y.Z [--to-php=X.Y] [--scan-only] <Vendor>_<Module>"
disable-model-invocation: true
---
Use the `magento2-tools:magento2-module-upgrade` skill, forwarding these arguments verbatim: $ARGUMENTS

Do not skip BC-break analysis or the plan-approval gate; the skill's normal flow applies.
